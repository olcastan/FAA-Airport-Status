class Airport:
	def __init__(self, name, code, city, state, temp, delay):
		self.name = name
		self.code = code
		self.city = city
		self.state = state
		self.temp = temp
		self.delay = delay
